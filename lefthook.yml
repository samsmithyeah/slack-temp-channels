pre-commit:
  parallel: true
  jobs:
    - name: lint & format
      run: npx biome check src/ tests/
    - name: typecheck
      run: npm run typecheck
    - name: dead code
      run: npm run knip
    - name: test
      run: npm test
